---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: formats.command.hdfs.stackable.tech
spec:
  group: command.hdfs.stackable.tech
  names:
    categories: []
    kind: Format
    plural: formats
    shortNames: []
    singular: format
  scope: Namespaced
  versions:
    - additionalPrinterColumns: []
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: "Auto-generated derived type for FormatCommandSpec via `CustomResource`"
          properties:
            spec:
              properties:
                force:
                  nullable: true
                  type: boolean
                name:
                  type: string
              required:
                - name
              type: object
            status:
              nullable: true
              properties:
                finishedAt:
                  description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                  format: date-time
                  nullable: true
                  type: string
                startedAt:
                  description: Time is a wrapper around time.Time which supports correct marshaling to YAML and JSON.  Wrappers are provided for many of the factory methods that the time package offers.
                  format: date-time
                  nullable: true
                  type: string
              type: object
          required:
            - spec
          title: Format
          type: object
      served: true
      storage: true
      subresources:
        status: {}
